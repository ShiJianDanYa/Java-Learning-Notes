---
title: 字符串分离
tags:
  - java
  - 实践
  - 字符串分离
date: 2026-06-04
创建时间: 2026-06-04 19:55:02
修改时间: 2026-06-04 19:55:02
星期: 星期四
难度: ⭐️⭐️
知识点:
关联:
---
# 📝 字符串分离
## 📌 题目要求
> 键盘录入任意字符串，请按长度为8拆分  
> 每个输入字符串并进行输出长度  
> 不是8整数倍的字符串请在后面补数字  
> 0空字符串不处理
> **示例**：
> 输入：abcdabcda  
> 输出：第一行：abcdabcd  
>       第二行：a0000000

## 🧠 解题思路分析
**需求拆解 和 解题思路**
1. 第一步：接收用户输入 ,定义变量存储字符串长度
2. 第二步：♾️循环
3. 第三步：循环截取字符串长度是 8,然后截取从 8 开始到数组长度,赋值给字符串
4. 第四步：判断长度是否小于 8,循环补 0,开始是剩余字符长度,结束条件是小于8

## 💻 核心代码

```java
// 关键代码片段

//接收用户输入  
    Scanner sc = new Scanner(System.in);  
    System.out.println("请输入字符串：");  
    String str = sc.next();  
    //得到长度  
    int len = str.length();  
    //循环截取能够=8的字符串  
    while(true){  
       if(len <= 8){  
          //补0  
          for(int i = str.length();i<8;i++) {str += "0";}  
          System.out.println(str);  
          break;  
       }else{  
          //截取 8个  
          System.out.println(str.substring(0,8));  
          //截取后的长度  
          str = str.substring(8);  
          //截取后的长度  
          len = str.length();  
       }  
    }  
}
```

## 🧪 运行结果

```text
输入：
123456789
输出：
12345678
90000000

进程已结束，退出代码为 0
```

##  🐛 调试记录 & 踩坑

| 问题现象 | 报错/日志 | 原因分析 | 解决方案 |
| -------- | --------- | -------- | -------- |
|          |           |          |          |
|          |           |          |          |

## ⚠️ 易错点 / 注意事项

* 条件判断顺序
* 边界值处理

## 📖 教师反馈 & 优秀解法
### 标准/更优解法
解法一
```java
//标准解法代码
//接收用户输入  
Scanner sc = new Scanner(System.in);  
System.out.println("请输入字符串：");  
String str = sc.next();  
//2.遍历字符串，每8个字符打印一行  
for(int i = 0;i < str.length();i++){  
    //获取字符串里面的每一个字符  
    char c = str.charAt(i);  
    //打印字符，不换行  
    System.out.print(c);  
    //判断，打印8次，才进行换行  
    //为了避免第一个i=0时,  
    // 0%8=0,第一个就自动换行,  
    // 所以i+1  
    if((i+1)% 8 == 0){  
       //换行  
       System.out.println();  
    }  
}  
//后面补零  
int lastLineCount = str.length() % 8;  
//判断是否需要补零  
if(lastLineCount != 0){  
    //在最后一行补多少个0  
    int count = 8 - lastLineCount;  
    //循环打印  
    for (int i = 0;i < count;i++) {  
       System.out.print("0");  
    }  
}
```
解法二
```java
//接收用户输入  
Scanner sc = new Scanner(System.in);  
System.out.println("请输入字符串：");  
String str = sc.next();  
//先补零  
//计算最后一行有多少个字符  
int lastLineCount = str.length() % 8;  
//计算最后一行补多少个0  
int count = 8 - lastLineCount;  
//补0零  
if(count != 0){  
    // 获取最后一行补的0  
    String line = "00000000".substring(0, count);  
    // 把最后一行补的0拼接到字符串的后面  
    str = str + line;  
}  
//循环截取  
//每8个打印一行  
//截取是包头不包尾  
//第一次是 0~8 截取的就是 0~7//第二次是 8~16 截取的就是 8~15
for(int i = 0;i < str.length();i+=8){  
    //每8个截取  
    System.out.println(str.substring(i,i+8));  
}
```
### 对比反思
我的方案 vs 参考答案，差距在哪里？是否可重构？
感觉还是我的比较好

## 🚀 扩展思考
* 如果改成...怎么做？
* 与之前某题的异同
## 📁 项目位置
