---
title: 字符求和
tags:
  - java
  - 实践
  - 
date: 2026-06-04
创建时间: 2026-06-04 20:34:45
修改时间: 2026-06-04 20:34:45
星期: 星期四
难度: ⭐️⭐️
知识点:
关联:
---
# 📝 字符求和
## 📌 题目要求
> 定义两个字符串，记录为非负整数，求它们的和
> **示例**：
> 输入：12395
>        133
> 输出：12528

## 🧠 解题思路分析
![[01_字符串计算_2026-06-02_16-44-35.png]]
**需求拆解 和 解题思路**
1. 第一步：先接收用户输出的字符串
2. 第二步：判断数据合法性,
3. 第三步：求两个字符串中最长的长度定义为 max,将两个字符都变为同样长度字符数组(以最长为蓝本,补零)
4. 第四步：定义第三个字符数组,长度为 两个数组中最长的+1,并定义变量 num 表示进位
5. 第五步: 开始计算
	1. 首先倒着遍历数组开始条件为 max-1,结束条件为>=0,
	2. 定义变量 temp 表示当前位置 两数相加结果 字符数组 1+字符数组 2+进位变量
	3. temp%10 求出位数,加到结果数组那里 注意结果数组是两个字符数组中最长的+1,所以就是 i+1
	4. num 变量就是进位是 temp/10
6. 创建StringBuffer对象sb
7. 数组转为字符串,判断最高位是否为零
8. 不是的 就将索引是 0 的结果数组添加到 sb 中
9. 循环提取元素到StringBuffer对象sb中 注意这里从1开始
10. 输出对象

## 💻 核心代码

```java
// 关键代码片段
//接收用户输入  
    Scanner sc = new Scanner(System.in);  
    System.out.println("请输入两个字符串：");  
    String str1,str2;  
    //判断输入数据合法性  
    while(true){  
       System.out.println("请输入第一个字符串：");  
       str1 = sc.next();  
       System.out.println("请输入第二个字符串：");  
       str2 = sc.next();  
       //循环截取字符  
       //定义变量 判断是否为数字  
       boolean flag1 = true;  
       boolean flag2 = true;  
       //判断字符1是否为数字  
       for(int i = 0;i < str1.length();i++){  
          char c = str1.charAt(i);  
          if(c < '0' || c > '9'){  
             System.out.println("字符一,输入数据不合法！");  
             flag1 = false;  
             break;  
          }  
       }  
       //判断字符2是否为数字  
       for(int i = 0;i < str2.length();i++){  
          char c = str2.charAt(i);  
          if(c < '0' || c > '9'){  
             System.out.println("字符二,输入数据不合法！");  
             flag2 = false;  
             break;  
          }  
       }  
       //数据合法,跳出循环  
       if(flag1 && flag2){  
          break;  
       }  
    }  
  
    //打印字符  
    System.out.println("字符一：" + str1);  
    System.out.println("字符二：" + str2);  
    //求最大值  
    int max = str1.length() > str2.length() ? str1.length() : str2.length();  
    //转换为int数组  
    int[] arr1 = copyData(str1, max);  
    int[] arr2 = copyData(str2, max);  
    //构建第三个数组 计算结果 长度为最大数组+1  
    int[] arr3 = new int[max+1];  
    //定义变量表示进位  
    int num = 0;  
    //核心：遍历数组，从个位（最大索引）位置开始计算  
    //arr1[i] + arr2[2] + 进位 = 结果  
    //结果的个位   sum数组当中  
    //结果的十位   进位num  
    for (int i = max-1; i >= 0; i--) {  
       //arr1[i] + arr2[2] + 进位 = 结果  
       int temp = arr1[i] + arr2[i] + num;  
       //结果的个位---sum数组当中  
       //arr3的长度是max+1  
       //所以接收的位置是i+1 才对  
       //栗子  
       //arr1 = 123  3位  
       //arr2 = 321  3位  
       //arr3是4位  
       //如果是i 那就是i=3 arr3[3]  
       //4 就被浪费了  
       arr3[i+1] = temp % 10;  
       //进位  
       num = temp / 10;  
    }  
    //最终结果  
    arr3[0] = num;  
    //转为字符串  
    StringBuffer sb = new StringBuffer();  
    //判断0索引中是否有数字
    //如果不是 0 那就添加到  StringBuffer对象sb中
    if(arr3[0] != 0){  
       sb.append(arr3[0]);  
    }  
    //这里就是从1开始 不是从0开始
    //循环提取元素到 StringBuffer对象sb中
    for (int i = 1; i < arr3.length; i++) {  
       sb.append(arr3[i]);  
    }  
    System.out.println(sb.toString());  
  
}  
  
//把字符串中的数据，放入到int类型的数组当中  
public static int[] copyData(String str, int len){  
    //定义数组长度为 len    int[] arr = new int[len];  
    //char 变为数字就是 数字减去 '0' 就是48  
    //比如 ascii 码 '1' = 49  '0' = 48    
    // 49 - 48  = 1    
    //为什么是这种 int length = str.length()    
    //而不是 int i = 0    
    //以 133 为例子 假设数组最长为5  
    //int i = 0,是从左边开始  结果为13300  
  
    //定义数组长度  
    int index = arr.length-1;  
    //从右边开始  
    for (int i = str.length()-1; i >= 0; i--) {  
       char c = str.charAt(i);  
       int num = c - '0';  
       arr[index] = num;  
       index--;  
    }  
    return arr;  
}
```

## 🧪 运行结果

```text
请输入两个字符串：
请输入第一个字符串：
12395
请输入第二个字符串：
133
字符一：12395
字符二：133
12528

进程已结束，退出代码为 0
```

##  🐛 调试记录 & 踩坑

| 问题现象 | 报错/日志 | 原因分析 | 解决方案 |
| -------- | --------- | -------- | -------- |
|          |           |          |          |
|          |           |          |          |

## ⚠️ 易错点 / 注意事项

* 注意计算的时候循环是逆着的,因为加法运算是从最低位开始
* 为什么结果数组是两个字符数组中最长的+1,举个例子 3 位数➕3位数最大是多少呢 最大的三位数是 999 那相加最大就是 999+999=1998 正好是 最大+1
* 两个字符变量相加的结果是 i +进位,在结果数组中可不是➕到 i 而是 i+1,因为结果数组的长度为两个数组最长的长度+1

## 📖 教师反馈 & 优秀解法
### 标准/更优解法
```java
	//标准解法代码
```
### 对比反思
我的方案 vs 参考答案，差距在哪里？是否可重构？

## 🚀 扩展思考
* 如果改成求乘积怎么做？
* 与之前某题的异同
## 📁 项目位置
